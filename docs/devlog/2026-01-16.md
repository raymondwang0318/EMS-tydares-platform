# Devlog — 2026-01-16

## 今日重點（Ingest v1 一次性定版）

本專案仍未上線，因此允許破壞性變更；但 ingest 架構已封版為 v1，後續不得語義漂移。

### v1 核心原則
- v1 唯一入口：`POST /ingest/{device_id}`（舊入口視為 REMOVED；禁止平行入口）
- ORDS handler 必須是 thin HTTP shell，不得實作商業邏輯
- 所有 ingest 語義委派 `ems_ingest_entrypoint.handle_ingest`
- Edge 必須遵守 `202/429/503 + Retry-After(秒)`，不得立刻重送
- 任何 ingest 行為變更需同步更新 Edge ↔ Central 契約與文件

## 交付內容

### Central（ORDS）
- 新增 `/ingest/{device_id}` thin handler（委派 DB entrypoint），並確保 `Retry-After` 為整秒。
- 破壞性移除舊入口 `/ingest/data`、`/ingest/media` 與其 deploy/scaffold。

### 文件（v1 Freeze + Normative/Implementation 分界）
- 在 repo 主要文件加入 v1 封版宣告與 guardrails。
- 新增 v1 封版索引頁，集中列出 Normative 與 Implementation 入口。

### Edge（契約文件）
- 由於本 repo 的 `edge/` 尚未有程式碼，先以契約文件方式鎖定 Edge 必須遵守的 v1 行為。

## Commit 清單（本機 main 目前較 origin/main 超前）

- e8de3ee docs: add ingest v1 index
- 949e323 docs: declare ingest v1 freeze (normative vs implementation)
- fec4f48 docs(edge): align with Central ingest v1 contract
- 5ae406a breaking: remove legacy ingest endpoints; v1 only /ingest/{device_id}
- 93a3106 ords: add /ingest/{device_id} thin handler
- 238c74c docs: add ingest implementation checklist
- cffde7a db: add ingest entrypoint glue package
- 71635aa docs: add normative ingest http entry flow
- 8087890 db: add global ingest overload check package
- ae63b77 db: add atomic token bucket rate limit package
- 1a1c72c db: add ingest constants and settings packages
- 2777ad0 docs: finalize Appendix A as implementation blueprint

## 下一步（可選）
- 把 Central 的 inbox store / stored|duplicate ACK 接上（目前 entrypoint 仍有 placeholder 區段）。
- Edge repo/程式開始落地後，依本 v1 契約實作 sender + retry/backoff。
