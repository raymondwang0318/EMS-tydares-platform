-- EMS-tydares-central / 01-core-platform
-- Users / roles scaffold (adjust to your security policy)

-- Example roles (create in DB as needed)
-- CREATE ROLE ems_core_reader;
-- CREATE ROLE ems_core_writer;

-- Grants (example; run after objects exist)
-- GRANT SELECT ON ems_site TO ems_core_reader;
-- GRANT SELECT, INSERT, UPDATE, DELETE ON ems_site TO ems_core_writer;
